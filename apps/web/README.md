# Web App

This app is the public-facing CONNECT Framework website built with Next.js and TypeScript.

## Folder Purpose

- `app/`: route-level UI (App Router pages and layouts).
- `src/`: shared typed configuration and utilities.
- `components/`: reusable UI components.
- `public/`: static assets.
